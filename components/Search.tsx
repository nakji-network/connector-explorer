import Data from "../contents/content.json";

function Search({data = Data["connectors"]}) {
    return (
        <div className="flex flex-col"></div>
        // TODO: somehow manage the state of the Grid component
    );
}

export default Search;
